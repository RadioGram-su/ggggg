Gram Radar Bot — split architecture (fixes Conflict forever)
============================================================

WHY Conflict happened:
  bothost used getUpdates (polling). Two processes = Conflict.

NEW setup:
  SITE  = /start /link via WEBHOOK
  BOTHOST = alerts only (watcher), NO polling

=== SITE .env ===

TELEGRAM_BOT_TOKEN=from_BotFather
TELEGRAM_WEBHOOK_URL=https://gramradar.org/api/telegram/webhook
TELEGRAM_WEBHOOK_SECRET=random_32_chars
TELEGRAM_AUTO_SET_WEBHOOK=1
EXTERNAL_TELEGRAM_BOT=1
BOT_API_SECRET=your_secret

bash fix.sh on VPS

=== BOTHOST env ===

BOT_API_SECRET=same
TELEGRAM_BOT_TOKEN=same
GRAMRADAR_API=https://gramradar.org
SITE_URL=https://gramradar.org

Start: node index.js
Upload: index.js, package.json, src/
